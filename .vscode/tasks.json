{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "docker-compose up --build",
            "type": "shell",
            "command": "docker-compose",
            "args": [
                "up",
                "--build"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [],
            "detail": "Ejecuta docker-compose up --build"
        }
    ]
}